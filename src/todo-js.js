import "./styles.css";

const onClickAdd = () => {
  //テキストボックスの値を取得し、初期化する。
  alert();
};

document
  .getElementById("add-button") //ID取得
  .addEventListener("click", () => onClickAdd()); //クリックした際に関数実行
